{
  "name": "flyflor-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "FLYFLOR_DEV=1 FLYFLOR_LOG=.flyflor-cli/logs/dev.log cargo watch --clear --why -w src -w Cargo.toml -w Cargo.lock -x 'run -- --dev'",
    "dev:node": "tsx scripts/dev.ts",
    "logs": "tsx scripts/logs.ts",
    "check": "cargo check",
    "test": "cargo test"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
